__pycache__/
*.pyc
*.xlsx
*.xls
.env
.env.*
!.env.example
node_modules/
local-server*.log
NUL

# Bucket index is built on the VM and published separately; never commit it here.
gcs-index/

# Working files of a local Rebuild from bucket; only the published asset is committed.
.truth-work/
